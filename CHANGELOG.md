# 版本更新信息汇总

## v0.3.2（2026-06-11）

### 表1 家庭成员

- **联系电话**：新增列（位于承包方代表之后），从表1 `ws.cell(4,10)` 提取
- **性别、身份证长度核对**：新增列（位于身份证号之后），自动校验并标红错误项
  - 身份证长度 ≠ 18 位 → 红底白字"错误"
  - 身份证号倒数第2位（性别码）与"性别"列不符 → "错误"
  - 关系推断性别不符（如孙子应为男、孙女应为女，本人/配偶跳过推断）→ "错误"
  - 全部匹配 → "正确"；无法判断 → 留空
- **合并逻辑修复**：变动区身份证号为真实值、确权区为"/"时，按姓名匹配后用变动区身份证号回填（修复 v0.3.0/v0.3.1 中同一个人出现两条记录的 bug）
- **调查记事提取优化**：遍历确权区和变动区所有行的第14列（v0.3.0 只读 section header 下一行，容易遗漏）

---

## v0.3.0（2026-06-09）

### 表1 家庭成员

- **分户支持**：变动原因含"分户"时，自动拆分为多个独立户口，承包方代表取分户后的新户主
- **跨人名分户**：同一人出现在多个分户组时，正确复制行并分配各自的分户原因
- **分、合户来源**：新增列，标注当前户的来源关系
  - 工具分户：从同一文件的分户变动自动识别，标注原始户编码
  - 跨文件分户：确权成员在其他文件中被"减少（分户）"时，标注来源户编码
  - 跨文件合户：变动区"增加"人员是其他文件的确权成员时，标注来源户编码
- **身份证号同步**：确权区身份证号为空或"/"时，用变动区的值补全
- **姓名纠正**：变动区姓名与确权区不同时（如错别字纠正），用变动区的值更新
- **合并逻辑优化**：变动区无身份证号时，按姓名在确权区中查找匹配（解决"/"无法匹配问题）
- **变动情况**：减少的人口不再删除，变动情况后附加原因说明（如"减少（死亡）"、"减少（分户）"）
- **户主变更**：新户主出现时备注"更换户主：新（原：旧）"
- **户内人口**：显示扣除减少人数后的实际户内人数
- **调查记事(附记)**：提取并显示调查记事信息
- **显示优化**：户内人口和调查记事只在每户首行显示，后续行清空

### 表2 承包地块

- **实际面积计算**：地块面积根据变动自动计算（增加则加，减少则减），不再显示原始确权面积
- **总面积更新**：确权总面积 = 所有地块实际面积之和
- **承包合同编号**：新增列，由文件编码加"J"生成（如 140802005204020022J）
- **四至简化**：去掉"变动后东至/西至/南至/北至"四个列，有变动的地块直接使用变动后的四至
- **分、合户来源**：与表1相同逻辑

### 通用

- 窗口标题显示版本号
- 承包方编码列：表1从合同编号去尾字母提取，表2从文件名前缀提取

---

## v0.2.1（2026-06-09）

- 支持分户：变动原因含"分户"时，分出来的户口单独成户
- 变动区身份证号或姓名有更正时同步更新到确权区
- 户内人口和调查记事只在每户首行显示
- 提取调查记事(附记)信息
- 户内序号改为户内人口（扣除减少人数）
- 户主变更时备注新老户主
- 减少的人口不再删除，变动情况后附加原因说明
- 确权区身份证号为空时用变动区的值同步

---

## v0.2.0（2026-06-03）

- 表2新增变动后四至列（东/西/南/北），无变动时填"无"
- 两表均新增承包方编码列
- 移除汇总表中的编号列
- 文件名匹配兼容无横线格式
- 表头搜索扩展到前5列，地块行不再严格要求序号
- 表1和表2变量作用域隔离
- 变动区出现本人时，承包方代表按变更后的新户主填写

---

## v0.1.0（2026-06-03）

- 原始版本：承包方家庭成员与承包地块信息提取工具
- 从 D:\农经全延保 下所有表1/表2 xlsx 文件中提取信息
- 支持确权区与变动区合并、减少人口处理
- GUI界面，支持文件夹浏览、提取、导出Excel
