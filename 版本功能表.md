# 承包信息汇总工具 — 版本功能表

> 源码: `C:\Users\26197\Documents\提取承包方家庭成员\`
> GitHub: https://github.com/tutuEic/contractor-extract-tool
> 技术栈: Python 3.14 + tkinter + openpyxl + PyInstaller

---

## 版本历史

### v0.3.5c (2026-06-13) — 当前版本

**文件名兼容性修复**

- 新增 `_is_biao1` / `_is_biao2`：正则匹配表1/表2文件
- 新增 `_normalize_biao_suffix`：去除（1）/(1)/副本 后缀再解析文件名
- 兼容格式：`表1.xlsx`、`表1（1）.xlsx`、`表1(1).xlsx`、`表1 副本.xlsx`
- 自动排除 Excel 临时文件（`~$` 开头）

---

### v0.3.5b (2026-06-13)

**变动区数据读取 + 户主识别修复**

- **Bug修复**: 变动区无序号行(C2为空)被 `_is_seq_number` 过滤掉
  - 变动区不再要求序号，确权区仍要求
- **Bug修复**: "户主"未被识别为新户主（代码只认"本人"）
  - "户主"统一归一化为"本人"
- **增强**: 关系转换表增加变体映射
  - `子` ↔ `儿子`、`女` ↔ `女儿`、`配偶` ↔ `妻子`、`父母` ↔ `父亲`
- **保护**: 变动区有"变更关系"标注时，跳过自动重算（避免覆盖正确值）

---

### v0.3.5 (2026-06-13)

**户主变更时自动重算家庭成员关系**

- 新增 `_RELATION_TRANSFORMS`：关系转换规则表
  - 支持 8 种户主变更类型：儿子/女儿/妻子/丈夫/孙子/孙女/父亲/母亲
- 新增 `_recalculate_relationships()`：户主变更后重算所有成员关系
- 示例：
  ```
  确权区: 张三(本人), 李四(妻子), 张五(儿子)
  变动区: 张五(户主)
  输出:   张五(本人), 张三(父亲), 李四(母亲)
  ```

---

### v0.3.4 (2026-06-11)

**合并时同步性别**

- **Bug修复**: 确权区身份证号为空"/"、变动区有真实身份证号时
  - 旧逻辑: 只回填身份证号，性别仍用确权区的值 → 核对失败
  - 新逻辑: 回填身份证号时同步性别 → 核对正确
- 示例：
  ```
  确权区: 性别=男, 身份证=/
  变动区: 性别=女, 身份证=142701196403292146
  v0.3.3: 身份证回填,性别仍为"男" → 核对失败
  v0.3.4: 身份证回填,性别同步为"女" → 核对正确
  ```

---

### v0.3.3 (2026-06-11)

- 版本号更新

---

### v0.3.2 (2026-06-11)

**新增列 + 身份证核对**

- 新增"联系电话"列（表1 ws.cell(4,10)）
- 新增"身份证+性别核对"列，自动检测：
  - 身份证长度 ≠ 18 → 红底白字"错误"
  - 第18位校验码错误（GB11643权重mod11）→ "错误"
  - 倒数第2位性别码与填写性别不符 → "错误"
  - 关系推断性别不符（如孙子→应为男）→ "错误"
  - 全部匹配 → "正确"
- 合并逻辑修复: person_key未命中时按姓名匹配
  - 修复身份证号从"/"变为真实值时漏合并的bug

---

### v0.3.1 (2026-06-11)

**精简打包**

- 优化 PyInstaller 打包配置
- 体积从 35MB (v0.3.0) 缩减至 13MB

---

### v0.3.0 (2026-06-10)

**初始版本**

- 从 `D:\农经全延保` 下所有表1/表2 xlsx 文件提取信息汇总
- 表1: 同一户中同一人确权区+变动区合并为一条，减少人口自动删除
- 表2: 提取地块确权登记信息及地块增减变动情况
- tkinter GUI 界面，支持选择文件夹、导出 Excel

---

## 输出列顺序

### 表1 (家庭成员)

| 序号 | 列名 | 数据来源 |
|------|------|----------|
| 1 | 所属组 | 子文件夹名 |
| 2 | 承包方编码 | 文件名前缀 |
| 3 | 承包方代表 | 表1 cell(4,5) 或变动区"本人" |
| 4 | 联系电话 | 表1 cell(4,10) |
| 5 | 发包方名称 | 表1 cell(3,1) |
| 6 | 户内人口 | 家庭成员总数 - 减少人数 |
| 7 | 家庭成员姓名 | 表1 cell(r,4) |
| 8 | 性别 | 表1 cell(r,6) |
| 9 | 身份证号 | 表1 cell(r,7) |
| 10 | 身份证+性别核对 | 自动计算 |
| 11 | 与承包方代表关系 | 表1 cell(r,9)，户主变更时自动重算 |
| 12 | 变动情况 | 变动区 cell(r,3) |
| 13 | 分、合户来源 | 变动原因解析 |
| 14 | 调查记事(附记) | 表1 cell(r,14) |

### 表2 (地块信息)

| 序号 | 列名 |
|------|------|
| 1 | 所属组 |
| 2 | 承包方编码 |
| 3 | 承包合同编号 |
| 4 | 承包方代表 |
| 5 | 联系方式 |
| 6 | 确权总面积(亩) |
| 7 | 地块总数 |
| 8 | 地块序号 |
| 9 | 地块名称 |
| 10 | 地块编码 |
| 11 | 地块面积(亩) |
| 12 | 东至 |
| 13 | 西至 |
| 14 | 南至 |
| 15 | 北至 |
| 16 | 变动情况 |
| 17 | 变动面积(亩) |
| 18 | 变动原因 |
| 19 | 分、合户来源 |

---

## 待处理

- [ ] 同音不同字姓名模糊匹配（如 廉永锋 vs 廉永峰）
- [ ] 配偶关系根据性别判断（当前"配偶"默认为"妻子"）
